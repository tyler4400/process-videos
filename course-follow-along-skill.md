---
name: course-follow-along
description: >-
  跟视频课程学习时的协作闭环方法论：文档先行 → 用户照文档实施（可偏离课程 / 修课程缺陷）
  → review 用户真实代码 → 回修文档对齐真实实现。Use when 用户要跟着课程视频系统学习、
  要先有"跟敲文档"再自己写代码、要 review 跟敲代码并修订学习文档，尤其是在自己真实项目里
  边学边写的场景（"跟着课程视频学习"、"先写跟敲文档我照着做"、"review 我的代码并改文档"、
  "整理某章节课程成学习笔记"）。本 skill 为上层方法论，底层"视频 → 字幕/截图"的提取依赖
  同仓库的 video-to-doc（SKILL.md）。
---

# Course Follow-Along：课程跟敲协作闭环

把"跟着视频课程学习"沉淀成一套可复用的**协作闭环**，而不是一次性把视频转成文档。

## 分层关系

- **底层 `SKILL.md`（video-to-doc，同仓库）**：管「视频 → 字幕 + 截图 → 跟敲文档」的预处理与读取。
- **本 skill（上层）**：管「怎么和用户协作，产出并持续迭代学习文档」。

> **路径约定**：本文件位于 `process-videos` 仓库根目录。下文凡形如 `./xxx` 的路径都相对本仓库根；需要运行脚本时，把 `<repo>` 替换为你 clone 仓库的实际路径（不要硬编码他人机器的绝对路径）。

## 协作闭环（核心）

每节 / 每组视频走一遍这个循环：

1. **文档先行**：基于视频（走底层 video-to-doc 读 `transcript.txt` + 关键帧）写出"跟敲文档"。此时用户**还没**写代码。
2. **用户实施**：用户看视频 + 按文档敲代码。**允许偏离课程**——用户可能用更优方案，或修正课程的过时写法 / bug。
3. **我 review**：用户写完，对照用户**真实提交的代码**做 review（找 bug、提改进、确认与文档/课程的差异）。
4. **回修文档**：把文档回修成对齐用户真实实现的版本，而非课程原版。

> 这是**循环**：复杂章节可能 1→2→3→4 后再补；简单章节文档先行、用户照做即可收尾。

## 核心原则

- **以用户的真实实现为准**：文档最终描述的是用户项目里**实际能跑的代码**，不是课程录制时的版本。
- **课程有缺陷就纠正**：课程方案过时 / 有 bug 时，文档写正确做法，并用差异块标注"课程是 X，我们改成 Y，因为 Z"。
- **不替用户写业务代码**：默认只写文档 + review，不动用户源码（除非用户明确要求）。

## 开新课前：对齐清单

接手一门新课程（或新章节）前，先与用户确认下列项，并记录到该学习项目里（如 `docs/<课程>/_约定.md`，或在对话中确认）。**这些会随课程变化，默认值只是起点。**

| 项 | 说明 | 例（lego 课程） |
|---|---|---|
| 视频源目录 | 课程视频 + `video-notes-cache/` 所在 | `<课程>/第22章 .../` |
| **课程源码** | 课程配套的**原项目**源码，理解课程"标准答案"用 | 课程附带的 `lego-admin` 源码 |
| **学习源码** | 用户自己**跟敲的项目**，文档代码以它为准 | monorepo 的 `packages/craft-admin` |
| 输出目录 | 学习文档存放位置 | `docs/imooc/` |
| 文件命名 | 命名规则 | 中文、无空格、按主题合并，如 `22-2_3-动态表单` |
| 文档结构 | 用默认模板还是定制 | 默认 4 段 |
| 差异维度 | 要标注哪些差异 | Nuxt4 / 依赖版本 / Monorepo 三维 |
| 分工节奏 | 文档先行 / 代码先行、批量范围 | 文档先行，用户分批指定范围 |

> 换一门课维度会变——比如 React 课的差异维度可能变成「课程版本 vs 你装的版本」「CRA vs Vite」等。

## 默认文档结构（可被每门课覆盖）

4 段式（从 lego 真实项目跟敲场景沉淀）：

```markdown
# <主题>（<视频区间>）

## 一、概述
<本节/本组要做什么、最终效果、涉及文件清单>

## 二、核心知识点 + 演示复盘
<按功能模块组织；代码直接用「学习源码」的真实路径写；
 给最终状态，避免"先写错再改"的返工>

## 三、与课程的差异速查
<就近用引用块标注差异：>
> ⚠️ <差异类型>：课程是 X，我们是 Y，因为 Z。

## 四、易错点 + 关键 API 速查
<跟敲踩坑 + 本节核心 API/配置速查表>
```

差异标注分类（按课程调整）：

- `⚠️ <框架版本> 差异`：如 Nuxt 3 → Nuxt 4
- `⚠️ 依赖版本差异`：课程录制版本 → 用户实际安装版本（第三方库跨多版本时尤其重要）
- `⚠️ Monorepo / 工程差异`：项目结构、共享包、lint 等上下文差异

一致时**不输出**"无差异"，只在有差异时标注。

## review 阶段怎么做

用户实施完、让我 review 时：

1. 读用户真实代码（学习源码里本节相关文件）。
2. 三方对照：**课程做法** vs **文档先行版** vs **用户实现**。
3. 输出分级反馈：
   - 🔴 必改：逻辑 bug、安全问题
   - 🟡 建议：更好的写法、与课程的取舍
   - 🟢 可选：风格、优化
4. **回修文档**：把"二、演示复盘"的代码替换成用户真实实现；在"三、差异速查"补「我们和课程为何不同」；记录用户修掉的课程 bug。
5. 最后用 `ReadLints` 检查改过的文档。

## 引用底层 video-to-doc

读视频、定位缓存、跑预处理，**全部走同仓库的底层 skill**，不要重复造轮子：

- 读取规范、缓存结构、踩坑细节：见 `./SKILL.md`
- 预处理脚本（缓存未就绪时先跑）：`<repo>/preprocess-videos.sh "<视频目录或单视频>"`

底层关键点（仅提醒，细节看 `./SKILL.md`）：

- 先找 `video-notes-cache/<视频名>/transcript.txt`，**别重新转写**。
- 字幕说"说了什么"，截图说"写了什么"，**关键帧必看**。
- 警惕跨视频"返工陷阱"（讲师留到下节补的代码，别在前一节写成最终版）。

## 踩坑（闭环特有，叠加在底层之上）

1. **别拿课程版当最终版**：文档先行只是"草稿"；用户实施后必须回修，否则文档会和用户项目脱节。
2. **差异要写根因**：不只写"课程是 X 我们是 Y"，要写**为什么**（版本演进 / 课程缺陷 / 工程约束），否则日后看不懂取舍依据。
3. **纯口头 / 无代码小节**：需求分析、总结类视频可只写概述，不强凑 4 段。
4. **合并粒度**：强相关的连续小节合并成一篇（如"上传组件四部曲"），但单篇别太长，必要时拆 2 篇。

## 产出物清单

- ✅ 学习文档（放对齐清单约定的输出目录）
- ✅ review 分级反馈 + 回修后的文档
- ✅ 文档无 lint 错误（`ReadLints`）
- ❌ 不替用户写业务代码（除非明确要求）
- ❌ 不重复底层 video-to-doc 的视频处理逻辑
